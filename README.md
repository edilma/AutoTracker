# AutoTracker
C# MVC .Net Core Web Application to keep track of maintenances and modifications of a user’s car.  The app uses Models, Views, Controllers, ViewModels 
for passing the information to the backend. 
Uses dependency injection for handling database transactions and LINQ to make queries and retrieve data from the data sources. 

